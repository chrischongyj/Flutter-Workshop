import 'package:flutter/material.dart';

class YourLibrary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Your Library", style: TextStyle(color: Colors.white))
    );
  }
}